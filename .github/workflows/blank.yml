name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Create controls file
      shell: bash
      run: |
        echo $PATH
        stat -f '%m' FHEM/98_NIBE_HEATPUMP.pm
