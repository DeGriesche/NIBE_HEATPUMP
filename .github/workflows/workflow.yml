name: update_controls

on:
  push:

jobs:
  linux:
    name: Update controls file
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node_version: [8.x]
    steps:
    - uses: actions/checkout@v1
    - name: script
      run: sudo ./prepare_update.sh
      shell: bash
